import ModuleConnection from '@/components/ModuleConnection/ModuleConnection';

export const metadata = {
  title: "Группы",
};

const Groups = () => <ModuleConnection id={'groups'} />

export default Groups;
